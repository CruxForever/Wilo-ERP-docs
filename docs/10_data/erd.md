# ERD

This page mirrors the ERD overview diagram. The visualization is rendered directly from the shared DOT definition at `erd/schema.dot`.

<div class="graphviz" data-graphviz-src="{{ base_url }}/erd/schema.dot">
  <em>Loading ERD graph...</em>
</div>

> Update `docs/erd/schema.dot` to change the structure of the diagram.
